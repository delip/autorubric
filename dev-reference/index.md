# AutoRubric Development Reference

A Python library for evaluating text outputs against weighted criteria using AI judges such as LLMs and decision models.

**For detailed documentation, examples, and usage guides, see docs/.**

This reference is split into focused pages for gradual disclosure — load only the page you need. Start here for the package map, then follow the [Reference Map](#reference-map) below.

## Package Structure

```
src/autorubric/
├── __init__.py             # Public exports
├── dataset.py              # DataItem, RubricDataset
├── decision.py             # DecisionModelConfig, DecisionModelClient (optional typesafe-sdk), framing + answer mapping
├── escalation.py           # replay_escalation, escalation_stats, calibrate_escalation, EscalationPoint, EscalationCurve (offline cascade tools)
├── eval.py                 # EvalRunner, EvalResult, evaluate()
├── llm.py                  # LLMConfig, LLMClient, ThinkingConfig, classify_grading_error
├── prompts.py              # Centralized prompt definitions
├── rate_limit.py           # RateLimitPool: per-provider, per-event-loop rate limiting
├── rubric.py               # Core Rubric class (criteria + optional guidelines), rubric file formats
├── scoring.py              # score_reports() shared weighted-scoring core
├── types.py                # Criterion, LengthPenalty, ensemble types, etc.
├── utils.py                # JSON parsing, length penalty utilities
├── graders/
│   ├── __init__.py         # Grader exports
│   ├── base.py             # Abstract Grader base class
│   └── criterion_grader.py # Unified grader (single/ensemble/few-shot/cascade), EscalationConfig
├── meta/
│   ├── __init__.py         # Meta-rubric evaluation exports
│   ├── _evaluate.py        # evaluate_rubric_standalone, evaluate_rubric_in_context
│   ├── _improve.py         # ImprovementRunner, improve_rubric, building blocks
│   ├── _display.py         # Rich display utilities
│   └── data/               # Meta-rubric JSON files
└── metrics/
    ├── __init__.py         # compute_metrics, result types
    ├── _compute.py         # Main compute_metrics implementation
    ├── _types.py           # MetricsResult, CriterionMetrics, etc.
    ├── _helpers.py         # Verdict extraction helpers
    └── distribution.py     # EMD, KS test, bias metrics
```

## Reference Map

Each page has a keyword-dense summary so you can route directly to the one that answers your question. The **Load when** line says what the page is for, and the **File location** line gives its path.

### Type Catalog

Quick-reference catalog of every public and internal type/function, organized by module: core types in `types.py` (`Criterion` and its `worst_scored_option`/`worst_option_among`/`na_option_index`/`with_guaranteed_na_option` methods, `CriterionOption`, the `CriterionVerdict` enum `MET`/`UNMET`/`CANNOT_ASSESS` and `_binary_worst_verdict`, `CriterionReport`, `EvaluationReport`, `EnsembleEvaluationReport` (`judge_scores: dict[str, float | None]`), `JudgeVote`, `MultiChoiceJudgeVote` with nullable `selected_index`/`selected_label`, `EnsembleCriterionReport` (`_compute_agreement`), `LengthPenalty`, `FewShotConfig` (`include_reason` shows `DataItem.ground_truth_reasons`), `FewShotExample` (`reason`), `TokenUsage`; probabilistic-judge fields `probabilities`/`confidence`/`superseded`/`escalated`; `reason`/`final_reason: str | None`; `_setstate_with_field_defaults` — older pickles restore added defaulted fields at their defaults, also on `JudgeMetrics`), grader types (`Grader` — `grade(..., *, guidelines=None)` forwarding to a `judge` that accepts it, abstract `judge`/`aggregate` unchanged, `_takes_guidelines`/`_warn_ignores_guidelines`; `CriterionGrader` — `judge_model_config`, `judge(..., *, guidelines=None)`, deprecated `llm_config`, `_clients`/`_decision_clients`; `escalation`, `judge_ids`, `_escalation_judges`, `is_ensemble`, `_check_escalation_names`, `_escalation_names_checked`; grader-owned copy `_escalation` and class-level defaults `_escalation`/`_escalation_names_warned`; repeated-`judge_id` `FutureWarning` and `_repeated_judge_ids`; `JudgeSpec` — stored `llm_config`, `judge_model_config` alias/property, overloaded `__init__`; `EscalationConfig` — frozen cascade config `judges`/`threshold`/`per_criterion`, `threshold_for`, `dataclasses.replace` re-validation, module functions `_check_thresholds`/`_threshold_for`/`_warn_unknown_threshold_names` shared with the replay; `CriterionResult`/`JudgeCriterionResults` (full-length `criterion_results` with `None` cells, `role` `primary`/`escalation`, `total_usage`/`total_cost`); `_escalates`; `_ensemble_evaluation_report`; `_failed_judgment_result`; `FewShotConfig`), rubric and dataset types (`Rubric` — keyword-only `guidelines` property, blank → `None` via `_normalize_guidelines` (`utils.py`), loaders `from_dict`/`from_json`/`from_yaml`/`from_file` via `_from_data` (criteria from the overridable `validate_and_create_criteria` hook, guidelines from `_rubric_guidelines`, `guidelines=` passed to `cls` only when set and declared by its `__init__` (`_accepts_guidelines(cls, through_kwargs=False)`; `**kwargs` does not count), else set through the `guidelines` property for legacy-`__init__` and `**kwargs` subclasses) for the list, `sections`, `criteria`, `rubric` and `{"guidelines", "criteria"}` forms, `_guidelines_field`/`_criteria_entries`; `DataItem` per-item rubric with its own guidelines, `ground_truth_reasons` (last field, aligned with `ground_truth`, checked in `__post_init__`, dropped from items `fill_ground_truth(force=True)` re-grades); `RubricDataset` — `add_item(..., *, ground_truth_reasons=None)`, `_serialize_rubric` dict form only with guidelines, `"ground_truth_reasons"` key only when set, JSON/file the only serializers, legacy byte-identity goldens `tests/dataset/test_legacy_reserialization.py`), LLM types (`LLMConfig`, `LLMClient` with `cache_namespace`, `ThinkingConfig`, `ErrorCategory`, `classify_grading_error`, `_classify_typesafe_error`), decision-model types in `decision.py` (`DecisionModelConfig` — `KW_ONLY`, `binary_framing`/`ordinal_framing`/`decision_threshold`/`input_cost_per_token`, `__post_init__` field validation; `api_base` checked at client build via `_check_base_url` (`httpx2.URL` parse, absolute http(s), no whitespace/query/fragment/credentials, `_shown_url` never repeats a URL with `@`) and `extra_headers` via `_check_extra_headers` (`h11.Request`) that the SDK does not set itself (`_SDK_OWNED_HEADERS`); `BinaryFraming`/`OrdinalFraming` `noul`/`noul_framed`/`choice`/`score`; internal `DecisionModelClient` — `system_one`/`token_usage`/`completion_cost`/`base_url`/`host`/`rate_limit_key`, connection slots (`_lease_sdk_client`, `_DEFAULT_CONNECTIONS`), the HTTP-environment check (`_new_http_client`, `TypeSafeAPIConnectionError`), `_is_valid_api_key`, `_check_base_url` (`_SYSTEM_ONE_PATH`, `httpx2.InvalidURL`), `TYPESAFE_API_KEY`/`TYPESAFE_BASE_URL`; `build_state` (with `guidelines=`)/`build_question`/`build_questions`/`question_id`/`answer_to_report`/`selection_confidence`), eval types (`EvalConfig`, `EvalResult`, `ItemResult`), and metrics types (`MetricsResult` with its handling-mode provenance `cannot_assess_mode`/`na_mode` and aggregate scalars `n_samples`/`mean_krippendorff_alpha`/`criterion_phi`/`macro_accuracy`/`micro_kappa`/`coverage_stats` plus the per-item-heterogeneous `pooled_by_scale: list[PooledScaleMetrics]`, the pooled rubric-point type `PooledScaleMetrics` (`scale_type`/`n_points`/`exact_accuracy`/`value_rmse`/`value_mae`/`value_spearman`/`value_pearson`/`n_abstain` + binary-only `kappa`/`phi`/`precision`/`recall`/`f1`/`confusion_matrix`), `CriterionMetrics` with `confusion_matrix`/`fpr`/`fnr`/`phi`/`is_degenerate`/`coverage_stats`, `OrdinalCriterionMetrics`, `NominalCriterionMetrics` — both migrated to a single `ConfusionMatrix` (`option_labels` removed) plus `is_degenerate`/`coverage_stats`, `JudgeMetrics` (+ `phi`/`confusion_matrix`; score fields `score_rmse`/`score_mae`/`score_spearman`/`score_kendall`/`score_pearson`/`bias` `None` only for `None` `judge_scores` entries; cascade `coverage` `full`/`escalated` + `n_pairs`, always serialized), the unified `ConfusionMatrix` and `CoverageStats` (`n_total`/`n_covered`/`n_errored` counts plus `coverage`/`judge_abstain_rate`/`gt_abstain_rate`/`union_exclusion_rate`/`error_rate`) typed diagnostics, `NAStats`, `CannotAssessStats`, plus the `CannotAssessMode`/`NAMode` literals `exclude`/`as_unmet`/`as_category`; note `AgreementSummary` has been removed). Each entry documents fields, which scalar metrics are `float | None`, the inter-judge agreement fields (`krippendorff_alpha`/`fleiss_kappa`), the φ (Matthews correlation coefficient) fields, and the `na_`/`ca_`-prefixed abstention diagnostics. It also lists rubric-improvement types from `meta/_improve.py` (`ImprovementConfig`, `_reject_decision_models`/`_check_eval_llm`/`_check_revision_llm`/`_get_eval_llm_config`, `_not_an_llm`) and the meta-rubric judgment types + full meta-rubric function roster. Cascade replay types in `escalation.py`: `EscalationPoint` (frozen pydantic; `threshold`/`per_criterion`/`escalation_rate`/`dm_accuracy_kept`/`dm_accuracy_escalated`/`fallback_accuracy_escalated`/`metric`/`cost_usd`/`compute_seconds`/`calibration_fingerprint`), `EscalationCurve` (frozen pydantic; `points`, `best(max_escalation_rate, tolerance)`, `to_dataframe`), `replay_escalation`, `escalation_stats`, `calibrate_escalation(dataset, dm_result, llm_result, *, thresholds, metric, per_criterion, min_pairs_per_criterion, cannot_assess_config, normalize)`, `_calibration_fingerprint`/`_replayed_fingerprint`, helpers `_selected`/`_fitted_thresholds`/`_warn_global_threshold`/`_DEFAULT_THRESHOLDS`/`_covered_items`/`_cascade_pairs`/`_paired_items`/`_checked_dm_judge_id`/`_checked_llm_judge_ids`/`_is_decision_model_vote`/`_judged_report`/`_escalated_report`/`_replayed_item`/`_recorded_item`/`_run_name`/`_decision_model_vote`/`_labels`/`_abstains`/`_vote_prediction`/`_final_prediction`/`_accuracy`/`_metric_value`.

**Load when:** you need the name, fields, methods, module location, or `float | None` nullability of any type/class/enum/function — core, rubric, grader, dataset, LLM, decision-model, eval, metrics, rubric-improvement, or meta-rubric — the semantics of the probabilistic-judge fields (`superseded`/`escalated`, `judge_scores` `None` by role), the `EscalationConfig` cascade config, the offline cascade types (`EscalationPoint`, `EscalationCurve`, `replay_escalation`, `escalation_stats`, `calibrate_escalation`), the `JudgeSpec` alias surface, `Rubric.guidelines` and the rubric file formats, `DataItem.ground_truth_reasons` (validation, dataset serialization, what `fill_ground_truth` does with them) and the few-shot config/example types, or to find which module a type lives in.

**File location:** `dev-reference/types.md`

### Grading Flow & Score Calculation

The end-to-end grading pipeline: `Rubric.grade()` delegates to `CriterionGrader` (a single judge, `judge_model_config=`, is an "ensemble of 1"), which at `judge()` start normalizes the rubric **once** into an *effective rubric* (`_effective_criterion`) — with `auto_na_option=True` (default) every multi-choice criterion is guaranteed an NA abstain option via `Criterion.with_guaranteed_na_option()`, a pure same-length/order transform that keeps `criterion_idx` and the shuffle RNG key aligned and never mutates the user rubric; `auto_na_option=False` gives forced-choice. Judge identity: a `judge_id` repeated in `judges=` conflates those judges (clients, few-shot examples, shuffle RNG, `judge_scores`, per-judge metrics) and warns `FutureWarning` via `_repeated_judge_ids` (grading unchanged; `ValueError` next major version, as in a cascade). Rubric guidelines plumbing: `Rubric.grade` passes `guidelines=` only when set (and only to a `grade` that accepts it); keyword-only `Grader.grade(..., *, guidelines=None)` forwards to a `judge` accepting `guidelines`/`**kwargs` (`_accepts_guidelines`, per-instance cache `_takes_guidelines`/`_guidelines_support` in `_instance_dict`), else the legacy warning "<GraderClass> ignores rubric guidelines" once per instance (`_warn_ignores_guidelines`, `_guidelines_warned` set only after the warning is issued, so warnings-as-errors raise on every call; `_caller_stacklevel`/`_INTERNAL_DIRS`); `_normalize_guidelines` (blank → none); per-item rubrics via `get_item_rubric`; `CriterionGrader.judge(..., *, guidelines=)`; `GUIDELINES_BLOCK`/`_guidelines_block` at the start of every LLM user prompt (`build_user_prompt`/`build_few_shot_user_prompt`/`build_multi_choice_user_prompt`/`build_multi_choice_few_shot_user_prompt`, keyword-only `guidelines`); `state["guidelines"]` first + `DECISION_MODEL_GUIDELINES_INSTRUCTION`. Few-shot examples (LLM judges): `_prepare_examples` → `_select_examples_for_criterion` / `_select_multi_choice_examples` (`_balanced_selection`/`_balanced_pick`, `_label_to_option_index`); each example's reason is its training item's `DataItem.ground_truth_reasons` entry (`_ground_truth_reason`, no RNG draw), rendered only under `FewShotConfig.include_reason=True`, so prompts are byte-identical otherwise (`tests/graders/test_few_shot_reasons.py`). Covers per-judge dispatch (`_judge_all_criteria_for_judge`: LLM judges call per criterion via `_judge_single_criterion` and `asyncio.gather()`; decision models (`DecisionModelConfig`) make one request per item via `_judge_all_criteria_with_decision_model` and `DecisionModelClient.system_one`), binary vs multi-choice response formats (`binary_response_format`/`CriterionJudgment`, `multi_choice_response_format`/`MultiChoiceJudgment`, meta overrides with `affected_criteria` and `[Affects: #1, #3]` injection via `_inject_affected_criteria()`, `_require_llm_reason()`), and vote aggregation (`majority` head-count vs `weighted`, plus independent `ordinal_aggregation`/`nominal_aggregation`; `_aggregate_votes`/`_aggregate_multi_choice_votes`, `_join_vote_reasons()`). Judge-call failure routing via `classify_grading_error()` maps `infrastructure`/`parse` → `CANNOT_ASSESS`/`na=True` (excluded under `CannotAssessStrategy.SKIP`) and `unknown` → conservative worst case; forced-choice no-NA abstain → `na=True` with `selected_index=None`; TypeSafe exceptions (`TypeSafeError`; `TypeSafeBadRequestError`/`TypeSafeUnprocessableEntityError`/`TypeSafeAPIResponseValidationError` → `parse`) via `_classify_typesafe_error`; `is_error`; the one failure helper `_failed_judgment_result`. Extended-thinking preservation copies `judgment.explanation`→`reason` and `judgment.reasoning`→`reasoning`; the provider schema strips the unused `reasoning` slot via `_provider_response_format()` so strict-mode backends (Groq) don't force non-OpenAI models (Llama/Qwen) to emit it. Prompt text: `MET_DEFINITION`/`UNMET_DEFINITION`/`CANNOT_ASSESS_DEFINITION`/`NEGATIVE_MET_DEFINITION`/`NEGATIVE_UNMET_DEFINITION` build `GRADER_SYSTEM_PROMPT_DEFAULT`; byte-identity goldens `tests/test_prompt_goldens.py`; `DECISION_MODEL_TASK_INSTRUCTION`/`DECISION_MODEL_THINKING_OUTPUT_TASK_INSTRUCTION`/`DECISION_MODEL_GUIDELINES_INSTRUCTION`/`DECISION_MODEL_REFERENCE_INSTRUCTION`/`DECISION_MODEL_CRITERION_PREFIX`; guidelines goldens (block + baseline golden). Decision-model framing and answer mapping (`decision.py`): `build_state` (`_has_thinking_output_sections`, `_THINKING_SECTION`/`_OUTPUT_SECTION`), `build_questions`/`build_question`, `question_id`, `_question_kind`, `_framed_instructions`, the `binary_framing`/`ordinal_framing` question table, inexpressible criteria (`_MAX_CHOICE_OPTIONS`, `_MIN_SCORE_LEVELS`/`_MAX_SCORE_LEVELS`), `answer_to_report` (Noul `decision_threshold` tie → `_binary_worst_verdict`; Score `round` snap via `_snapped_level`), `selection_confidence`, the recorded-run fixture (`recorded_jev.json`; its scope, and the request differences a re-run shows). Decision-model judges in `CriterionGrader`: clients (`_clients`/`_decision_clients`; missing SDK, bad key, `api_base`/`TYPESAFE_BASE_URL` or HTTP environment (`SSL_CERT_FILE`/proxies) fail at construction, a later-broken one → `TypeSafeAPIConnectionError`/`infrastructure`), one request per item, failure scope, billing (`CriterionResult` usage/cost on the first result, `JudgeCriterionResults.total_usage`/`total_cost`, `input_cost_per_token`), votes, mixed ensembles, construction-time checks (few-shot `ValueError`, `_prepare_examples` for LLM judges only, response-format `ValueError`, LLM-only-settings `UserWarning` when a setting differs from its default (`system_prompt`/`multi_choice_system_prompt` not `None`, `shuffle_options=False`; never for a mixed ensemble or cascade), `stacklevel=2`). Confidence cascade (`escalation=EscalationConfig(judges, threshold, per_criterion)`): exactly one decision-model primary, LLM escalation judges in `_escalation_judges`, unique `judge_id`s, the grader's own re-validated copy `_escalation = dataclasses.replace(escalation)` (in-place config changes), class-level defaults `_escalation`/`_escalation_names_warned` (init-skipping subclasses, older pickles), `judge_ids` for `EvalRunner` progress behind the `hasattr(grader, "_judges")` gate; escalated set `_escalates` (error, `CANNOT_ASSESS`/NA abstention, `confidence < threshold_for(criterion)` by name); `_judge_escalated_criteria` with the original `criterion_idx` (prompts equal a non-cascade grader with the same `seed`/`judge_id`); carrier `role` + full-length `None` cells; `superseded`/`escalated`, aggregation over non-superseded votes only, never a fallback to the decision model; `judge_scores` `None` by role; unknown `per_criterion` names (`_check_escalation_names`) warned once per grader or once per batch — `EvalRunner.run` and `fill_ground_truth` via `_escalation_names_checked` (`_ESCALATION_NAMES_CHECKED`). Offline Cascade Replay, Diagnostics and Calibration (`escalation.py`): zero-call `replay_escalation(dm_result, llm_result, escalation, *, per_criterion, cannot_assess_config, normalize)` reusing the live `_escalates`/`_threshold_for`/`_check_thresholds`/`_warn_unknown_threshold_names`/`_ensemble_evaluation_report`/`score_reports` (items paired by `item_idx`; `_judged_report`; kept = the decision-model run's report, escalated = the LLM run's recorded aggregate + superseded vote via `_escalated_report`, so a panel stays aggregated with the LLM run's settings; exactness conditions: each run graded by a `CriterionGrader` configured as the cascade for that stage (`judges=escalation.judges`, `seed`/`judge_id`/weights, `shuffle_options`, system prompts, few-shot settings, `auto_na_option`, aggregation settings; only the criteria checked); validation `ValueError`s incl. `_is_decision_model_vote` and one LLM judge set; cost/time pro-rated by the escalated share (run total pooled over the LLM run's pairs, a failed decision-model item's counted unescalated; `_replayed_item`), `token_usage` `None`, marked "estimated cost and time" in `experiment_name` (`_run_name`); length penalty not re-applied; `_calibration_fingerprint`/`_replayed_fingerprint`/`_recorded_item` same-data `UserWarning`), and `escalation_stats(result, dataset, *, metric)` → `EscalationPoint` over pooled pairs (per-item rubrics too; cascade-shape check `_decision_model_vote`; labelled pairs `_labels`/`_abstains`; `_vote_prediction`/`_final_prediction`/`_metric_value` (any `numbers.Real` but `bool`, NaN → `None`); decision-model errors/abstentions counted in `escalation_rate` only; `dm_accuracy_escalated`/`fallback_accuracy_escalated` paired on the deferred pairs (confidence-escalated, final verdict not abstaining; `TestTheDeferredPairs`), fallback = final verdicts, `cost_usd`/`compute_seconds` sums; `_covered_items`/`_cascade_pairs`), and `calibrate_escalation(dataset, dm_result, llm_result, *, thresholds, metric, per_criterion, min_pairs_per_criterion, cannot_assess_config, normalize)` → `EscalationCurve` (zero-call sweep, default `_DEFAULT_THRESHOLDS` 0.00–1.00 step 0.02; each point = `escalation_stats` of a replay scored with the cascade's `cannot_assess_config`/`normalize` (score-level metrics such as `score_rmse`) + `threshold`/`per_criterion`/`calibration_fingerprint` of the dataset's graded items; cost, same-data (not recognized on `from_experiment` runs) and exactness caveats; metric higher-better, NaN read as `None` by `_metric_value`, `criterion_accuracy` on the per-criterion path (`_criterion_level_scalars`: binary pairs alone when any) vs pooled on `_compute_per_item_pooled_metrics` (every scale, predicted abstention left out, abstaining label a miss), `pooled_by_scale` callables; selection rule `EscalationCurve.best(max_escalation_rate, tolerance)` = least escalating point within `tolerance` (inclusive up to rounding, `math.isclose`) of the best metric under the budget, `None`/NaN metrics passed over, ties higher metric → lower threshold → earlier, one implementation `_selected`; `to_dataframe`; per-criterion fitting `_fitted_thresholds`: coordinate-wise on the run's own `metric` from the global sweep's `best`, names in order of first appearance (pinned over rubric order) (never lowers it; fallback abstentions and multi-choice criteria count as `metric` counts them), `min_pairs_per_criterion` labelled pairs inclusive, thin/metric-undefined `UserWarning` via `_warn_global_threshold`, points = swept global threshold + fixed fitted overrides that `replay_escalation(point)` reproduces; input checks `_check_thresholds`/`_is_positive_int`; experiments parity test `tests/escalation/test_cascade_parity.py` on the recorded fixture `cascade_parity.json`/`cascade_fixture.py` through `ScriptedLLM` at library threshold `2 * tau`; the fixture's builder `build_cascade_fixture.py` (run by hand; subset by stride; `check_recorded_summary`/`check_simulation`/`check_library_escalation`) and its consistency tests `test_cascade_fixture.py` (`simulated_point`)). Score Calculation centers on the single core `score_reports(reports, config, normalize=True)` in `scoring.py` (shared by the live grader, `Rubric.compute_score`, `RubricDataset.compute_weighted_score`) — the weighted-sum/clamp formula, negative-weight-only fallback, length penalty, `_abstain_contribution` for `SKIP`/`ZERO`/`PARTIAL`/`FAIL`, and the empty-denominator `0.0` of an item whose every judgment failed (either judge kind).

**Load when:** you need to understand how `Rubric.grade` routes to the grader, what a repeated `judge_id` does, how rubric guidelines reach each judge kind (forwarding, legacy-grader warning, LLM prompt block, decision-model state and clause), how few-shot examples are drawn and where the reasons they show come from, NA auto-injection / effective-rubric normalization, response formats and `affected_criteria` injection, vote aggregation and `final_reason`, judge-call failure classification/routing, extended-thinking preservation, the verdict-definition constants and prompt goldens, how a decision model's state, questions and answers are built and mapped, how a decision-model judge runs inside `CriterionGrader` (alone, in a mixed ensemble, or as a confidence cascade's first stage — what escalates, how escalation judges are called and their votes aggregated) and which settings it rejects or ignores, how a cascade is replayed offline from a decision-model run and an LLM run (`replay_escalation`: what makes it exact, what it validates, how cost and time are estimated and marked, the same-data warning) and diagnosed (`escalation_stats`: how its accuracies are defined) and calibrated (`calibrate_escalation`/`EscalationCurve.best`: the sweep, the selection rule, per-criterion fitting, the cost/same-data/exactness caveats, the parity with the recorded cascade simulation), or how the single `score_reports` core computes weighted scores and applies `CannotAssessStrategy`.

**File location:** `dev-reference/grading-flow.md`

### Multi-Choice Criteria

Multi-choice criteria end to end: `scale_type` `ordinal` (weighted kappa) vs `nominal` (unweighted kappa), explicit per-option `value` (0–1) and `shuffle_options=True` to mitigate position bias (LLM judges), and NA options (`na: true`) as the structural analog of binary `CANNOT_ASSESS` flowing through `score_reports` under the configured `CannotAssessStrategy`. Covers the first-class abstain channel: the auto-injected `CANONICAL_NA_OPTION`, NA-label rendering via `_render_options`/`_label_signals_na` in `prompts.py`, and the unconditional `MULTI_CHOICE_SYSTEM_PROMPT` NA section. Details scale-aware, option-driven empty/refusal handling — failure/lowest-quality option when one describes an empty submission (ordinal, scored ≈0) → else NA (nominal, SKIP-excluded) → else closest forced-choice option — and the same option-shape rule for contradictory/ambiguous submissions (the judge never sees weights, so scoring applies the weight sign downstream via option `value`). Specifies the three orthogonal ensemble aggregation knobs (`aggregation`, `ordinal_aggregation`, `nominal_aggregation`) on a central/conservative/permissive axis with the full value table, including nominal `unanimous` abstaining via the genuine NA option, and the multi-choice-only per-criterion override `Criterion.aggregation` (ignored for binary criteria). Closes with uniform, deterministic, weight-sign-aware tie-breaking: score-minimizing outcome + lowest-index tie-break, `_binary_worst_verdict` / `Criterion.worst_option_among`; and decision-model multi-choice framing (`DecisionModelConfig.ordinal_framing` `Choice`/`Score`, no shuffling, `str(original_index)` probability keys, Score `round` snap `_snapped_level`, Score cannot abstain, no guidelines clause — `state["guidelines"]` only).

**Load when:** you need ordinal vs nominal scales, option `value`/shuffling and position bias, NA/abstain handling, how empty/refusal and contradictory submissions are scored, the ensemble aggregation knobs and their central/conservative/permissive values (and the per-criterion `Criterion.aggregation` override), how ensemble ties are broken, or how a decision model poses multi-choice criteria.

**File location:** `dev-reference/multi-choice.md`

### Abstention Handling

How abstention is handled across scoring and metrics for both binary `CANNOT_ASSESS` and multi-choice NA. Documents the four CannotAssessStrategy scoring strategies (`SKIP`, `ZERO`, `PARTIAL`, `FAIL`) applied by the single `score_reports` core, plus judge-call failure routing (`infrastructure`/`parse` → abstain excluded under SKIP, `unknown` → conservative worst case, forced-choice no-NA abstain → `na=True` with `selected_index=None`) and decision-model abstention (which `DecisionModelConfig` framings can abstain: binary `choice` with `CANNOT_ASSESS_DEFINITION`, multi-choice `Choice` NA; Noul `decision_threshold` ties and `Score` never abstain; in a cascade abstentions and errors always escalate (also in `replay_escalation`; `escalation_stats` keeps them out of the decision-model accuracies), and all-abstaining escalation votes abstain the criterion, never falling back; its `error` via `_aggregate_error` only when every escalation vote failed, `None` for genuine `CANNOT_ASSESS`/NA answers). Details metrics-time `na_mode=NAMode` handling (`exclude`, `as_unmet` via `Criterion.worst_scored_option()`, `as_category` which raises `ValueError` for ordinal NA), effective-criterion reconstruction via `Criterion.with_guaranteed_na_option()` when an out-of-range or `None` prediction is observed, and the `as_worst`→`as_category` rename. Describes the mode-independent `NAStats` counts (`na_count_true`/`na_count_pred`/`na_false_positive`/`na_false_negative`/`na_kappa`) and the binary parallel `CannotAssessStats` block with `ca_`-prefixed counts. Explains the metrics-time `CoverageStats` wiring (exclude-mode only, via `_build_coverage_stats`): per-criterion + aggregate coverage on a RAW denominator (`items_with_ground_truth + n_errored_items`) with union-exclusion, the `n_errored_items` counter at the error-skip site, the grading-error warning, and the top-level `n_samples`; and items no judge judged (every criterion `is_error`: scored like any item, also in `_compute_per_item_pooled_metrics`; only `ItemResult.error` counts as errored). Closes by explaining why `CANNOT_ASSESS` (epistemic, MET-vs-UNMET) and NA (option-space "not applicable") are tracked as **two separate types** despite sharing the SKIP path and dichotomized-kappa structure.

**Load when:** you need how CANNOT_ASSESS/NA strategies affect the scoring denominator, how judge-call failures route to abstain vs worst-case, which decision-model framings can abstain, how metrics treat NA via `na_mode`, when `as_category` is refused, how auto-injected/`None` abstains normalize, how exclude-mode `CoverageStats` / `n_errored_items` / `n_samples` are wired, how an item whose every judgment failed is counted, or why CANNOT_ASSESS and NA are separate types.

**File location:** `dev-reference/abstention.md`

### Metrics

The metrics layer: inter-judge agreement — Krippendorff's α (`krippendorff_alpha`, level-aware ordinal/nominal, built from a per-criterion reliability matrix tolerating missing/errored cells) and complete-case Fleiss' κ (`fleiss_kappa` via `statsmodels`, uniform-rater subjects) — both ground-truth-independent and computed even with zero GT-paired samples. States the framework principle that prediction-vs-ground-truth categorical agreement is always Cohen's kappa (binary `CriterionMetrics.kappa`, ordinal `weighted_kappa` quadratic, nominal `kappa`), with orthogonal abstain agreement via `NAStats.na_kappa` and `CannotAssessStats.ca_kappa`. Details aggregate/per-judge scalar parity: `criterion_precision`/`recall`/`f1` are binary MET-vs-rest (`None` for multi-choice-only rubrics), `criterion_accuracy`/`mean_kappa` generalize, sharing `_criterion_level_scalars` (now a 7-tuple also yielding `phi`+`micro_kappa`) + `_mean_or_none`; per-judge metrics mirror the aggregate field-for-field (1-judge == aggregate invariant, incl. `phi`). Covers the Matthews correlation φ helper `_mcc_or_none` (mandatory `len(set(...)) < 2 → None` guard against sklearn's misleading 0.0; φ≡κ on matched marginals, φ>κ on positive-rate drift), the binary 2×2 `ConfusionMatrix` + `fpr`/`fnr`/`phi`/`is_degenerate` via `_build_binary_2x2_confusion_matrix`, the per-judge 3×3 `["MET","UNMET","CANNOT_ASSESS"]` `ConfusionMatrix` via `_build_binary_judge_confusion_matrix` (RAW pre-filter codes pooled across binary criteria; aggregate == elementwise sum), the aggregate `criterion_phi`/`macro_accuracy`/`micro_kappa`/`mean_krippendorff_alpha`, `is_degenerate` on ordinal/nominal (`weighted_kappa`/`kappa is None`), and the score-collapse + degeneracy warnings. Exhaustively specifies the cross-cutting **"undefined → `None`, never a fabricated `0.0`"** invariant across Cohen kappa (`_kappa_or_none` NaN handling), empty-data criteria, correlations (`_compute_correlation`, <3 samples / constant arrays), distribution stats, systematic bias / Cohen's d, inter-judge agreement (`validate_agreement`, `mean_agreement`), item-level bootstrap CIs for all rubric types (`_compute_bootstrap_ci`, `accuracy_ci`/`kappa_ci`/`rmse_ci`), grade-failure `None` scores, `None` `judge_scores` entries (`_compute_judge_metrics` score fields `score_rmse`/`score_mae`/`score_spearman`/`score_kendall`/`score_pearson`/`bias`, `_coefficient`, `_fmt_opt`, a judge whose every vote failed keeping its float entry, changed-judge-set `ValueError`), and extraction-failure `None` mapping. Cascade reports: the per-judge missing-vote mask `judge_missing` (a missing binary vote is not `UNMET`, a missing multi-choice vote is not an NA prediction and never triggers `_needs_na` NA-option reconstruction; also fixes mixed judge sets on resumed runs), `superseded` votes as the decision model's predictions, escalation-judge `JudgeMetrics.coverage` (`"full"`/`"escalated"`, by role: every `judge_scores` entry `None`) + `n_pairs` (escalated-subset size) with score-level fields `None`, the `summary()` suffix `(escalated subset, N pairs)`, cascade-only `to_dataframe()` columns `judge_coverage`/`n_pairs`, `coverage`/`n_pairs` always dumped (a full panel's dump gains them at `"full"`/`null`; `ADDED_JUDGE_METRICS_FIELDS`/`dumped_today`, `NEW_JUDGE_METRICS_FIELDS`, parity R4/R7 `KNOWN_NEW_METRICS_KEYS`), Fleiss (`_build_fleiss_row`) skipping `superseded` votes (κ `None` for a cascade) while Krippendorff α (`_build_alpha_cell`) keeps them, cascade diagnostics in `escalation_stats` (pooled pairs, an `EscalationPoint`, no `MetricsResult` block) and `calibrate_escalation` (each sweep replay measured by `compute_metrics` into an `EscalationCurve`), and the full-panel bit-identity golden (`tests/golden/metrics/full_panel_metrics.json`, `capture_metrics_fixtures.py`; floats compared up to `FLOAT_TOLERANCE` via `assert_same` across interpreters and platforms). Also covers the **per-item heterogeneous-rubric pooled path**: `compute_metrics(..., per_item_metrics="auto"|"pooled"|"per_criterion")` dispatches to `_compute_per_item_pooled_metrics` (gated by `_has_heterogeneous_rubrics`/`_rubric_signature`) when the dataset has no global rubric and item rubrics differ (HealthBench) → `per_criterion=[]` + `MetricsResult.pooled_by_scale: list[PooledScaleMetrics]` (per-scale exact-accuracy + unified value pool; binary-only κ/φ/P/R/F1/confusion; the homogeneous per-item case stays per-criterion via the `get_item_rubric` score-step fix).

**Load when:** you need to understand or modify metric computation — including the per-item heterogeneous pooled path (`per_item_metrics`/`pooled_by_scale`/`PooledScaleMetrics`/`_compute_per_item_pooled_metrics`) — inter-judge agreement (Krippendorff α vs Fleiss κ), pred-vs-truth Cohen's kappa across binary/ordinal/nominal, the Matthews φ helper `_mcc_or_none` / `criterion_phi` / per-judge φ, the binary 2×2 and per-judge 3×3 `ConfusionMatrix` builders, `fpr`/`fnr`/`is_degenerate`, `macro_accuracy`/`micro_kappa`/`mean_krippendorff_alpha`, score-collapse / degeneracy warnings, aggregate vs per-judge scalar parity, undefined per-judge scores, cascade per-judge metrics (missing votes, superseded votes, escalation-judge `coverage`/`n_pairs`, Fleiss vs α on cascades; cascade diagnostics live in `escalation_stats`/`EscalationPoint`, not in `MetricsResult`), item-level bootstrap CIs, abstention-agreement kappas, or the "undefined → None" invariant covering correlations, distribution stats, bias, grade-failure scores, and extraction failures.

**File location:** `dev-reference/metrics.md`

### Reproducibility & Seed Coordination

How all non-LLM randomness is made deterministic. `CriterionGrader(seed=...)` controls option shuffling and few-shot selection; when `None` the `master_seed` is auto-generated so shuffles are always pinned. Per-call shuffle RNGs derive from `(master_seed, content_hash, criterion_idx, judge_id)` via SHA-256 (concurrency-safe, no shared mutable state) using `_derive_shuffle_rng()` in `criterion_grader.py`; an unset `FewShotConfig.seed` is coordinated from the master seed, and few-shot selection RNGs derive from `(few_shot_seed, criterion_idx, judge_id)` with a constant `FEW_SHOT_DOMAIN` in the item-key slot; an example's reason (`DataItem.ground_truth_reasons`) draws no randomness, so the same seed draws the same examples with or without reasons. `CriterionReport.shuffle_order` records the per-criterion permutation, and `ExperimentManifest.grader_config` persists `master_seed`/`shuffle_options`/`auto_na_option` plus per-judge `judge_kind`, `temperature` (`null` = provider default) and decision-model `binary_framing`/`ordinal_framing`/`decision_threshold`/`api_base_host` (the host resolved from `api_base`/`TYPESAFE_BASE_URL`; never the key, URL path or `extra_headers`; the base URL is no secret: failed reports' `error` shows it) (`_decision_model_judge_entry`) via `_serialize_grader_config` in `eval.py`, a cascade's `escalation` key (escalation judges via `_llm_judge_entry`, `threshold`, `per_criterion`, read from the grader's own copy `grader._escalation`), and the known `grader._normalize` gap. Cascade escalation judges keep each criterion's original `criterion_idx`, so their prompts match a non-cascade grader with the same `seed` and `judge_id` (`"escalation"` for a bare `LLMConfig`). With `auto_na_option=True`, a criterion that lacked an NA option gains one appended at the end, so its `shuffle_order` grows by one. `DecisionModelConfig` non-determinism and `cache_enabled`.

**Load when:** you need how seeds/RNGs are derived and coordinated, how option shuffling and few-shot selection are made deterministic and concurrency-safe, what is persisted for checkpoint reproducibility (including the per-judge and cascade manifest keys), how to make an LLM run match a cascade's escalation prompts (e.g. to replay it with `replay_escalation`), how auto-injected NA options affect a criterion's shuffle permutation, or how decision-model non-determinism and the response cache affect reproducibility.

**File location:** `dev-reference/reproducibility.md`

### Improvement Loop Artifacts

What the rubric improvement loop persists. When `save_artifacts=True` and `artifacts_dir` is set, it writes per-iteration files — `rubric-iter-{NN}.json` (criteria array, or the `{"guidelines", "criteria"}` dict form for a rubric with guidelines; `_save_rubric`/`_rubric_artifact_data`/`_rubric_criteria_data`), `eval-iter-{NN}.html` (meta-rubric eval report, always generated regardless of `display`), `iter-{NN}.json` (rich per-iteration JSON: quality report, issues, validation samples, revision prompts/response) — plus run-level `improvement_report.html` (consolidated, always generated; Final Rubric panel shows guidelines) and `summary.json` (full run metadata, config snapshot, per-iteration summary; `original_rubric`/`final_rubric`). Also notes that `revise_rubric()`, `validate_agreement()`, and `validate_ground_truth()` accept a private `_capture` parameter used to collect these artifacts. Rubric guidelines in the meta paths: the meta-judge sees them (`_rubric_for_meta_judge`, `_meta_judge_instruction`/`_GUIDELINES_REF_INSTRUCTION`, `_rubric_summary`); revised rubrics carry them unchanged (`_revised_rubric`); the revision LLM sees them read-only, never revises them (`RUBRIC_REVISION_GUIDELINES_BLOCK`, `_with_guidelines_block`); byte-identical prompts without guidelines (`tests/meta/test_meta_prompt_goldens.py`, `tests/golden/meta/`).

**Load when:** you need to know what files the improvement loop writes to disk, what `save_artifacts`/`artifacts_dir` produce, the naming/contents of per-iteration vs consolidated artifacts, how `_capture` feeds artifact collection, or how rubric guidelines flow through meta-rubric evaluation, revision and the loop's artifacts.

**File location:** `dev-reference/improvement-loop.md`

### Key Conventions & Public Exports

The framework's cross-cutting conventions: all graders return `EnsembleEvaluationReport`; the `judge_model_config` keyword and its deprecated alias `llm_config` (`DeprecationWarning`, `TestSuiteWideDeprecationGuard`, `pytest_configure`'s `error::DeprecationWarning:autorubric` filter); deprecating a silently-wrong behaviour with `FutureWarning` before refusing it (repeated `judge_id`s); roles a decision model cannot fill — meta-rubric evaluation and rubric improvement (`_not_an_llm`, `_reject_decision_model_judges`, `_reject_decision_models`, `_check_eval_llm`/`_check_revision_llm`, `_get_eval_llm_config`, `ImprovementRunner.run`, `_reject_decision_model_grader`); rubric guidelines reach every judge and change nothing when absent (`Rubric.grade`/`Grader.grade` forwarding, "ignores rubric guidelines" legacy warning, `GUIDELINES_BLOCK`, `state["guidelines"]`, byte-identical prompts/requests and cache keys without them); `raw_score` is always populated regardless of `normalize`; judge-call failures route via `classify_grading_error()` (`infrastructure`/`parse` → `CANNOT_ASSESS`/`na=True` excluded under default SKIP, `unknown` → conservative worst case, forced-choice no-NA abstain → `na=True` with `selected_index=None`). Documents uniformly pydantic-native report serialization: `EnsembleCriterionReport`/`JudgeVote`/`MultiChoiceJudgeVote` are frozen pydantic models, so checkpointing collapses onto `model_dump(mode="json")`/`model_validate` like the single `CriterionReport` path, with `ItemResult.to_dict` keeping its envelope (`report_type` discriminator, `criterion_reports`, `judge_scores`, `token_usage`), `_serialize_ensemble_criterion_report`/`_deserialize_ensemble_report`, and `EnsembleCriterionReport.agreement` computed by a frozen-safe `model_validator(mode="after")`. Objects pickled by an earlier release restore fields added since at their defaults (`__setstate__ = _setstate_with_field_defaults` on `CriterionReport`/`JudgeVote`/`MultiChoiceJudgeVote`/`EnsembleCriterionReport`/`JudgeMetrics`; fixture `tests/golden/legacy/pickled_results.json`). Covers category-prefixed `error` round-tripping, `reason is None` (`_require_llm_reason`, `extract_issues`, `_format_error_criteria`), the additive probabilistic-judge vote fields, the cascade report shape (`escalated`, superseded decision-model vote first, final fields from non-superseded votes, `judge_scores` `None` for escalation judges; rebuilt offline by `replay_escalation`), extended-thinking `reasoning` round-tripping with legacy-checkpoint-tolerant field defaults, filtering `error is not None` in training pipelines, per-provider rate limiting via `LLMConfig.max_parallel_requests` and `RateLimitPool`'s per-event-loop semaphores (`get_semaphore`, `_maps_lock`; lookup by the running loop, pinned for a loop left open and for loops in two threads by `TestRateLimitPoolEventLoops`), the decision-model client `DecisionModelClient` (extra `autorubric[typesafe]` = `typesafe-sdk>=0.7.1` + `httpx2>=2.0.0` + `h11>=0.16`, `_import_typesafe_sdk`, key/URL resolution (`TYPESAFE_API_KEY`/`TYPESAFE_BASE_URL`) via `typesafe_sdk.constants` `API_KEY_ENV`/`BASE_URL_ENV`/`DEFAULT_BASE_URL` and `_is_valid_api_key`, `_check_base_url` (query/fragment/credentials rejected; `_SYSTEM_ONE_PATH` appended by the SDK) via `httpx2.URL`/`httpx2.InvalidURL` (IDNA 2008 host, IPv4), `extra_headers` checked by `h11` (`_check_extra_headers`, `_http_layer_sends`) minus the SDK's own (`_SDK_OWNED_HEADERS`, `TestHeadersTheSdkSets`), the HTTP environment (`_new_http_client` built at construction, `SSL_CERT_FILE`/`SSL_CERT_DIR`/`HTTPS_PROXY`/`ALL_PROXY` → `ValueError`; later breakage → `TypeSafeAPIConnectionError`/`infrastructure`; `TestHttpEnvironment`, `http_environment`/`HttpEnvironment`/`HTTP_ENVIRONMENT_FAILURES`), `AsyncTypeSafeClient` with a `RetryPolicy` retry budget vs `LLMConfig.retry_max_wait`, `_lease_sdk_client` (lease before the semaphore; close failures logged), connection slots outside the SDK (`http_client=httpx2.AsyncClient`, `httpx2.Limits(max_connections=...)`, `httpx2.Timeout(timeout, pool=None)`, `_DEFAULT_CONNECTIONS` = 100 for `max_parallel_requests=None`, `httpx2.PoolTimeout`; `TestConnections`, `http_network`/`InMemoryNetwork`), `rate_limit_key` (not `_normalize_to_provider`), `_open_response_cache`/`_canonical_json` cache with `use_cache`, `token_usage`/`completion_cost`/`input_cost_per_token`, manifest keys `judge_kind`/`api_base_host`), `DecisionModelConfig` judges sharing the one failure-report helper `_failed_judgment_result` with LLM judges, `stacklevel=2` caller attribution of `CriterionGrader.__init__` warnings, `.env` loading from the working directory at import (`load_dotenv(find_dotenv(usecwd=True))`), send-only-when-set `LLMConfig.temperature` (default `None` → provider default; the effective temperature is part of the `LLMClient` response-cache key), one run per experiment directory (`_start_fresh_checkpoint`: `resume=False`, a changed dataset or a missing manifest replaces the checkpoint), per-judge response-cache keys (`LLMClient(cache_namespace=...)`: each ensemble/cascade judge's `judge_id`, the lone judge's `_SINGLE_JUDGE_ID` key left plain), and the default guaranteed abstain option `CriterionGrader(auto_na_option=True)` vs `auto_na_option=False` forced-choice. Also states the **single-source metric-redundancy note** discipline: the conflation / different-geometry / κ−φ-gap notes live only in `MetricsResult.summary()`/`to_dataframe()`/docstrings and are never copied into the `meta/_display.py` Rich/HTML paths. Ends pointing to `src/autorubric/__init__.py` for the complete Public Exports list.

**Load when:** you need the framework-wide conventions — what graders return, the `judge_model_config` / `llm_config` deprecation, when a deprecation warns with `FutureWarning` rather than `DeprecationWarning`, how rubric guidelines reach graders and judges, which roles stay LLM-only, what a `None` reason means, what a cascade's report looks like, `raw_score`/`normalize` behavior, judge-call failure routing, how reports/votes serialize and round-trip across checkpoints (and restore from older pickles), where `error`/`reasoning` traces live, rate-limiting config and its event-loop scope, how the decision-model client resolves and checks its key and URL, retries within its budget, handles event loops, rate-limits, sizes its connection pool and queues requests outside the SDK, caches and prices requests (and the `typesafe` extra), where the `.env` is loaded from, when `temperature` is sent and how it enters the cache key, how judges' response-cache keys are scoped, what a fresh start does to an existing experiment directory, the default auto-NA behavior, the single-source metric-note discipline, or where public exports are listed.

**File location:** `dev-reference/conventions.md`

## How to update autorubric's dev reference

This reference uses **gradual disclosure**: `CLAUDE.md` `@`-imports only this `index.md`, so it loads every session as a lightweight router, while the eight detail pages load on demand when a link is followed. Preserve that property.

**Edit the page the change touches.** `types.md` for type/field changes, `grading-flow.md` for the grading pipeline or scoring core, `metrics.md` for metric computation, and so on. 

**Keep this index a router, not a copy.**

- Don't hyperlink the headings; give each page a **File location** line with its repo-relative path in backticks (e.g. `dev-reference/types.md`). Never use `@`-imports — an `@`-import would auto-load every page and defeat gradual disclosure.
- When you add, rename, or remove a page, update the **Reference Map** above with a keyword-dense TLDR, a **Load when** line, and a **File location** line. The TLDR is what makes a page discoverable, so name its key types, functions, config knobs, enum values, T-codes, and topics.
- When you add a new type, function, config knob, or T-code to a page, make sure the term also appears in that page's TLDR here — otherwise a reader scanning the index won't find it.
- Don't let the index grow toward monolith size: TLDRs + keywords route; the prose stays on the pages.

**Page conventions (so edits stay consistent).**

- Each page begins with the breadcrumb `[← Dev Reference Index](index.md)` and a blank line, then its content.
- A reference to another page's section becomes a cross-file link — `[Section Name](other-page.md)`. A reference to a section on the *same* page stays as italic emphasis (`*Section Name*`).
- **Dedup, don't drop facts.** The same mechanism is often restated in a type-table cell, an architecture page, and `conventions.md`. When trimming, consolidate to one canonical page and cross-link from the others — never delete a fact. Preserve verbatim every type/field/method/function name, enum value, table row, and code block.